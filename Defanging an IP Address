def defang_ip_address(address: str) -> str:
    """
    Replace every '.' with '[.]' in the given IP address string.
    Example: "1.1.1.1" -> "1[.]1[.]1[.]1"
    """
    return address.replace('.', '[.]')

# Example
print(defang_ip_address("1.1.1.1"))  # Output: 1[.]1[.]1[.]1
